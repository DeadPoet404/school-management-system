services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sms_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: sms_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sms_user -d sms_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./sms-core-backend
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: postgresql://sms_user:${POSTGRES_PASSWORD}@postgres:5432/sms_db?schema=public
      # ⚠️ CHANGE THIS IN PRODUCTION — generate with: openssl rand -base64 32
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: 8h
      NODE_ENV: production
      CORS_ORIGINS: http://localhost:3000
      PORT: "5000"
      RATE_LIMIT_WINDOW_MS: "60000"
      RATE_LIMIT_MAX_REQUESTS: "100"
      AUTH_RATE_LIMIT_MAX_REQUESTS: "5"
      LOG_LEVEL: info
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build: ./sms-core
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:5000/api
    depends_on:
      - backend

volumes:
  postgres_data:
